<ul class="providers list-inline">
  <% if devise_mapping.omniauthable? %>
    <% resource_class.omniauth_providers.each do |provider| %>
      <li>
        <%= link_to provider, omniauth_authorize_path(resource_name, provider) %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= render "devise/shared/links" %>
